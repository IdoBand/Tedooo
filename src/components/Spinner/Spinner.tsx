import { RingLoader } from "react-spinners";

export default function Spinner() {
    return (
        <div className="loading-container">
            <RingLoader color="rgb(60, 200, 200)"/>
        </div>
    )
}